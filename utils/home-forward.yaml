---
apiVersion: v1
kind: Endpoints
metadata:
  name: rauber-router
  namespace: utils
  labels:
    app: rauber-router
subsets:
  - addresses:
      - ip: 192.168.1.1
    ports:
      - name: https
        port: 4443
---
apiVersion: v1
kind: Service
metadata:
  name: rauber-router
  namespace: utils
  labels:
    app: rauber-router
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: https
      port: 443
      targetPort: https
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
  name: rauber-router
  namespace: utils
  labels:
    app: rauber-router
spec:
  rules:
  - host: rauber.srueg.ch
    http:
      paths:
      - backend:
          serviceName: rauber-router
          servicePort: https
        path: /
  tls:
  - hosts:
    - rauber.srueg.ch
