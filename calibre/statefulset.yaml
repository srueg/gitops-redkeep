apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: calibre
  namespace: calibre
  labels:
    app.kubernetes.io/name: calibre
    app.kubernetes.io/instance: calibre
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: calibre
      app.kubernetes.io/instance: calibre
  serviceName: ""
  template:
    metadata:
      labels:
        app.kubernetes.io/name: calibre
        app.kubernetes.io/instance: calibre
    spec:
      containers:
      - name: calibre
        image: docker.io/linuxserver/calibre-web:0.6.12-ls125@sha256:1c1a3de473f4dfacb40b8d12371d65c162dfd4cc64139cbb1fa806626e4de4c9
        imagePullPolicy: IfNotPresent
        env:
        - name: PUID
          value: "10001"
        - name: PGID
          value: "10001"
        - name: TZ
          value: Europe/Zurich
        - name: DOCKER_MODS
          value: linuxserver/calibre-web:calibre
        volumeMounts:
        - mountPath: /config
          name: data
        ports:
        - name: http
          containerPort: 8083
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 300
        readinessProbe:
          httpGet:
            path: /
            port: http
      enableServiceLinks: false
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: calibre
