apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: timemachine
  namespace: timemachine
  labels:
    app.kubernetes.io/name: timemachine
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: timemachine
  replicas: 1
  serviceName: timemachine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: timemachine
    spec:
      containers:
      - name: timemachine
        image: docker.io/mbentley/timemachine:smb@sha256:7fa829eac39fbbe355b0e1f81ec23646395966e866cd5ff4288e01c158c738ac
        env:
        - name: PASSWORD
          valueFrom:
            secretKeyRef:
              name: timemachine
              key: password
        ports:
        - name: smb
          containerPort: 445
          hostPort: 445
        volumeMounts:
        - name: data
          mountPath: /opt/timemachine
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes:
      - ReadWriteOnce
      storageClassName: zfs
      resources:
        requests:
          storage: 1Ti
      selector:
        matchLabels:
          usage: timemachine
